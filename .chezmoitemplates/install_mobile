# Update system
apt update
apt upgrade

# Install packages
apt install curl dnsutils git gnupg gzip inetutils mosh net-tools \
    openssh pinentry tmux vim zsh

# Cleanup
apt autoremove
apt autoclean

# Enable storage
[ ! -d "$HOME/storage" ] && termux-setup-storage
