# Update system
apt update
apt upgrade

# Install packages
apt install chezmoi curl dnsutils git gnupg gzip inetutils mosh net-tools \
    openssh pinentry tmux vim zsh

# Cleanup
apt autoremove
apt autoclean

# Enable storage
termux-setup-storage
