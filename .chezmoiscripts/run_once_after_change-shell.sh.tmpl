{{- if .workstation -}}
#!/usr/bin/env bash
# vim: filetype=sh
set -euo pipefail

if [ "$SHELL" != "$(command -v zsh)" ]; then
    chsh --shell "$(command -v zsh)"
fi

rm -rf -- \
    "$HOME/.bashrc" \
    "$HOME/.bash_history" \
    "$HOME/.bash_logout" \
    "$HOME/.bash_profile"
{{- end -}}
